{
    "name": "ffmpeg-prototype-root",
    "version": "1.0.0",
    "description": "Root package to manage frontend and backend",
    "scripts": {
        "postinstall": "cd frontend && npm install && cd ../backend && npm install",
        "dev": "concurrently \"npm run dev --prefix frontend\" \"wait-on http://localhost:5173 && npm start --prefix backend\"",
        "build": "npm run build --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^9.1.0",
        "wait-on": "^8.0.1"
    }
}